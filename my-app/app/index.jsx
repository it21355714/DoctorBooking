import React from 'react';
import AppNavigator from '../navigation/AppNavigator'; // Ensure the correct path

const index = () => {
  return <AppNavigator />; // No additional NavigationContainer here
};

export default index;
